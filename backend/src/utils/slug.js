import {query} from '../db.js'; export function slugify(s){return String(s||'').toLowerCase().trim().replace(/[^\w\s-]/g,'').replace(/[\s_-]+/g,'-').replace(/^-+|-+$/g,'')}; export async function uniqueSlug(base,table='products',ignoreId=null){let slug=slugify(base)||'item',i=1;while(true){const rows=await query(`SELECT id FROM ${table} WHERE slug=? ${ignoreId?'AND id<>?':''} LIMIT 1`,ignoreId?[slug,ignoreId]:[slug]);if(!rows.length)return slug;slug=`${slugify(base)||'item'}-${i++}`}}
