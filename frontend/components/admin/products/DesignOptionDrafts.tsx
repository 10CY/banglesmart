"use client";
import { Plus,Trash2 } from "lucide-react";
export type DesignDraft={client_key:string;label:string;sort_order:number;status:"active"|"inactive"};
export default function DesignOptionDrafts({designs,onChange}:{designs:DesignDraft[];onChange:(next:DesignDraft[])=>void}){
 const add=()=>onChange([...designs,{client_key:`design-${Date.now()}`,label:`Design ${designs.length+1}`,sort_order:designs.length,status:"active"}]);
 return <section className="rounded-xl border border-gray-200 bg-white p-6"><div className="flex items-start justify-between gap-4"><div><h2 className="text-base font-semibold text-gray-900">Design Options <span className="font-normal text-gray-400">(Optional)</span></h2><p className="mt-1 text-sm text-gray-500">Use this only when the same product has visually different patterns/designs. Design images can be managed after creation.</p></div><button type="button" onClick={add} className="inline-flex items-center gap-2 rounded-lg border border-gray-300 px-3 py-2 text-sm font-medium hover:bg-gray-50"><Plus size={16}/>Add design</button></div>{designs.length===0?<div className="mt-5 rounded-lg border border-dashed border-gray-300 p-6 text-center text-sm text-gray-500">No design options. The normal product gallery will be used.</div>:<div className="mt-5 space-y-3">{designs.map((d,i)=><div key={d.client_key} className="grid gap-3 rounded-lg border border-gray-200 p-4 md:grid-cols-[1fr_140px_44px]"><input value={d.label} onChange={e=>onChange(designs.map((x,n)=>n===i?{...x,label:e.target.value}:x))} placeholder={`Design ${i+1}`} className="rounded-lg border border-gray-300 px-3 py-2 text-sm outline-none focus:border-gray-600"/><select value={d.status} onChange={e=>onChange(designs.map((x,n)=>n===i?{...x,status:e.target.value as "active"|"inactive"}:x))} className="rounded-lg border border-gray-300 bg-white px-3 py-2 text-sm"><option value="active">Active</option><option value="inactive">Inactive</option></select><button type="button" aria-label="Remove design" onClick={()=>onChange(designs.filter((_,n)=>n!==i).map((x,n)=>({...x,sort_order:n})))} className="rounded-lg border border-red-200 text-red-600 hover:bg-red-50"><Trash2 size={16} className="mx-auto"/></button></div>)}</div>}</section>
}
