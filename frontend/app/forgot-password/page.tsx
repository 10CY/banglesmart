"use client";
import { FormEvent, useState } from "react";
import Link from "next/link";
import { customerApiFetch } from "@/lib/customerApi";
export default function ForgotPasswordPage(){const [email,setEmail]=useState('');const [message,setMessage]=useState('');const [loading,setLoading]=useState(false);async function submit(e:FormEvent){e.preventDefault();if(loading)return;try{setLoading(true);setMessage('');const r=await customerApiFetch('/customer/forgot-password',{method:'POST',body:JSON.stringify({email})});const j=await r.json();if(!r.ok)throw new Error(j?.message||'Unable to request reset.');setMessage(j?.message||'If the account exists, a reset link has been sent.');}catch(e){setMessage(e instanceof Error?e.message:'Unable to request reset.');}finally{setLoading(false);}}return <main className="flex min-h-screen items-center justify-center bg-[#faf6ee] px-4 py-12"><div className="w-full max-w-md rounded-3xl bg-white p-8 shadow-sm sm:p-10"><p className="text-xs font-semibold uppercase tracking-[.2em] text-[#c9a227]">Account Security</p><h1 className="mt-3 text-3xl font-semibold text-gray-900">Forgot password?</h1><p className="mt-2 text-sm leading-6 text-gray-500">Enter your account email and we’ll send a secure reset link.</p><form onSubmit={submit} className="mt-7 space-y-4"><input type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email address" className="w-full rounded-xl border border-gray-200 px-4 py-3 text-sm outline-none focus:border-[#c9a227]"/><button disabled={loading} className="w-full rounded-xl bg-gray-900 px-4 py-3 text-sm font-semibold text-white disabled:opacity-50">{loading?'Sending...':'Send reset link'}</button></form>{message&&<p className="mt-4 rounded-xl bg-[#f8f1e5] p-4 text-sm text-gray-600">{message}</p>}<Link href="/login" className="mt-6 block text-center text-sm font-semibold text-[#8f0828]">Back to login</Link></div></main>}
